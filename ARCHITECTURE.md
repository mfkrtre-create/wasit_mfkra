# معمارية مفكرة الوسيط

## الواجهة

- `app/page.tsx`: المصادقة وتحميل مساحة عمل المستخدم.
- `ui/`: تطبيق React RTL المرجعي: لوحة التحكم، العروض، الطلبات، OpenFreeMap، CRM، الحساب، وإدارة المنصة.
- `ui/lib/db.ts`: محول الحالة بين نموذج الواجهة ومساحة العمل المحفوظة.

## الخادم والبيانات

- Next.js Route Handlers تتولى المصادقة، AI، الصور، المشاركات، الإدارة، والحفظ.
- PostgreSQL يحفظ الحسابات والجلسات ولقطة JSONB معزولة لمساحة عمل كل مستخدم.
- كل قراءة أو كتابة للبيانات الخاصة تمر عبر جلسة HTTP-only ومعرف المستخدم.
- سلة المحذوفات تستخدم `deletedAt`، وتُنظف العناصر الأقدم من 30 يوماً عند تحميل المساحة.

## الذكاء الاصطناعي

- `/api/transcribe`: Groq `whisper-large-v3-turbo` لتفريغ الصوت العربي.
- `/api/extract-property`: Gemini بإخراج JSON مقيد بمخطط Zod، مع تعليمات مخصصة للهجة العقارية السعودية.
- `/api/search-listings`: Gemini يحول البحث الطبيعي إلى فلاتر، ثم تطبقها الواجهة على بيانات المستخدم.
- مفاتيح AI تبقى على الخادم فقط.

## الخرائط والمشاركة

- MapLibre GL JS يعرض بلاطات OpenFreeMap، مع دبابيس ملونة وبطاقة تفاصيل ورابط توجيه.
- روابط `/s/[token]` لقطات عامة منتقاة، مؤقتة أو قابلة للإلغاء، ويمكن أن تعرض الصورة عبر مسار عام مرتبط بالرمز.

## الترحيلات

`scripts/migrate.mjs` ينفذ ملفات `db/migrations` بالترتيب ويسجلها في `schema_migrations`. تشغيل الإنتاج ينفذ الترحيلات قبل Next.js.
